//create database structure --> using EF create model for back-end + append some model view field

//customize front-end(Html helper(DOM, attribute), keep state with model, ...)

//session(encode, decode)

//add extra helper, extension class for back-end

//login(session, cookie, model validation, viewBag)

//log-out(session)

//register(model state is valid, model validation, tempData and cookie can pass throuh different controler OR action, email notify)

//admin thong ke - chuyen cho Thanh lam

admin manage user, article(customer my col name by annotation, pagination)

// authorization module 1 phan

cms fix bug account ko load duoc combobox

test flow, logic code, review, tách hàm, tách hằng số, comment

user menu when login

register (insert ảnh, hash salt password)
https://jasonwatmore.com/post/2020/07/16/aspnet-core-3-hash-and-verify-passwords-with-bcrypt